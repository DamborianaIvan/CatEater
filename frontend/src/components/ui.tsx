import { useEffect, useRef, type ButtonHTMLAttributes, type ReactNode } from 'react'; import { X } from 'lucide-react';
export function Button({ children, loading, className = '', ...props }: ButtonHTMLAttributes<HTMLButtonElement> & { loading?: boolean }) { return <button className={`button ${className}`} disabled={loading || props.disabled} {...props}>{loading ? 'Procesando…' : children}</button>; }
export function Notice({ message, kind = 'error' }: { message?: string; kind?: 'error' | 'success' }) { return message ? <p className={`notice ${kind}`} role="alert">{message}</p> : null; }
export function Modal({ title, children, onClose }: { title: string; children: ReactNode; onClose: () => void }) { const ref = useRef<HTMLDivElement>(null); useEffect(() => { ref.current?.focus(); const key = (e: KeyboardEvent) => e.key === 'Escape' && onClose(); addEventListener('keydown', key); return () => removeEventListener('keydown', key); }, [onClose]); return <div className="overlay" role="presentation" onMouseDown={onClose}><section className="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title" tabIndex={-1} ref={ref} onMouseDown={(e) => e.stopPropagation()}><header><h2 id="modal-title">{title}</h2><button className="icon-button" onClick={onClose} aria-label="Cerrar"><X /></button></header>{children}</section></div>; }
export function PageState({ title, action }: { title: string; action?: ReactNode }) { return <div className="page-state"><p>{title}</p>{action}</div>; }
